<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hurrycaneblurryname.ryde">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.LoginScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.SignupActivity" />
        <activity android:name=".View.EditUserProfile" />
        <activity android:name=".View.DriverMainActivity" />
        <activity android:name=".View.RiderMainActivity" />
        <activity android:name=".View.AcceptedRequestActivity" />
        <activity android:name=".View.DriverInfoActivity" />
        <activity android:name=".View.RiderRequestInfoActivity" />
        <activity android:name=".View.OpenRequestActivity" />
        <activity android:name=".View.RiderRideInfoActivity" />
        <activity android:name=".View.RiderConfirmDriverActivity"></activity>

    </application>

</manifest>