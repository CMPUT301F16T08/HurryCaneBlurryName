<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hurrycaneblurryname.ryde">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.LoginScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.SignupActivity" />
        <activity android:name=".View.EditUserProfile" />
        <activity android:name=".View.RiderCurrentRequestsActivity" />
        <activity android:name=".View.DriverInfoActivity" />
        <activity android:name=".View.RequestHistoryActivity" />
        <activity android:name=".View.DriverOpenRequestsActivity" />
        <activity android:name=".View.RiderRideInfoActivity" />
        <activity android:name=".View.RiderConfirmDriverActivity" />
        <activity
            android:name=".View.RiderMainActivity"
            android:label="@string/title_activity_rider_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.DriverMainActivity"
            android:label="@string/title_activity_driver_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.AvailableDriversActivity" />
        <activity android:name=".View.NewRequestActivity" />
        <activity android:name=".View.HistoryRideInfo"></activity>
    </application>

</manifest>